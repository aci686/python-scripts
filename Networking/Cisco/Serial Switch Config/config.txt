common = no service pad
common = service timestamps debug datetime msec
common = service timestamps log datetime msec
common = service password-encryption
common = service sequence-numbers
common = #Hostname
common = logging buffered 8192 informational
common = no logging console
common = no logging monitor
common = #Username
common = aaa new-model
L = aaa group server radius L_ISE_Group
T = aaa group server radius T_ISE_Group
N = aaa group server radius N_ISE_Group
L = server name l-isepsn-01
T = server name t-isepsn-01
T = server name t-isepsn-02
N = server name n-isepsn-01
L = aaa group server radius FP_ISE_Group
N = aaa group server radius FP_ISE_Group
L = server name fp-isepsn-01
N = server name fp-isepsn-01
L = aaa authentication login default group L_ISE_Group group FP_ISE_Group local
T = aaa authentication login default group T_ISE_Group local
N = aaa authentication login default group N_ISE_Group group FP_ISE_Group local
L = aaa authentication dot1x default group L_ISE_Group group FP_ISE_Group
T = aaa authentication dot1x default group T_ISE_Group
N = aaa authentication dot1x default group N_ISE_Group group FP_ISE_Group
common = aaa authorization console
L = aaa authorization exec default group L_ISE_Group group FP_ISE_Group local 
T = aaa authorization exec default group T_ISE_Group local
N = aaa authorization exec default group N_ISE_Group group FP_ISE_Group local
L = aaa authorization network default group L_ISE_Group group FP_ISE_Group
T = aaa authorization network default group T_ISE_Group 
N = aaa authorization network default group N_ISE_Group group FP_ISE_Group 
common = aaa accounting update periodic 15
L = aaa accounting dot1x default start-stop group L_ISE_Group group FP_ISE_Group
T = aaa accounting dot1x default start-stop group T_ISE_Group
N = aaa accounting dot1x default start-stop group N_ISE_Group group FP_ISE_Group
L = aaa accounting exec default start-stop group L_ISE_Group group FP_ISE_Group
T = aaa accounting exec default start-stop group T_ISE_Group
N = aaa accounting exec default start-stop group N_ISE_Group group FP_ISE_Group
L = aaa accounting network default start-stop group L_ISE_Group group FP_ISE_Group
T = aaa accounting network default start-stop group T_ISE_Group
N = aaa accounting network default start-stop group N_ISE_Group group FP_ISE_Group
common = aaa server radius dynamic-author
L = client 10.21.11.100 server-key 7 12345678901234
T = client 10.21.8.105 server-key 7 12345678901234
N = client 10.21.20.250 server-key 7 12345678901234
L = client 10.2.0.2 server-key 7 12345678901234
T = client 10.21.9.105 server-key 7 12345678901234
N = client 10.2.0.32 server-key 7 12345678901234
common = aaa session-id common
common = clock timezone GMT 0 0
L = clock summer-time GMT recurring last Sun Mar 1:00 last Sun Oct 2:00
common = device-sensor filter-list cdp list cdp-list
common = tlv name device-name
common = tlv name address-type
common = tlv name capabilities-type
common = tlv name platform-type
common = device-sensor filter-spec cdp include list cdp-list
common = device-sensor accounting
common = device-sensor notify all-changes
common = no ip source-route
common = no ip gratuitous-arps
common = ip icmp rate-limit unreachable 1000
common = ip dhcp snooping vlan 1-4094
common = no ip dhcp snooping information option
common = ip dhcp snooping
common = no ip domain-lookup
common = ip domain-name this.domain
L = ip name-server 10.21.101.10
T = ip name-server 10.20.58.6
N = ip name-server 10.21.200.12
L = ip name-server 10.21.101.11
T = ip name-server 10.20.59.6
N = ip name-server 10.21.200.13
L = ip device tracking probe delay 5
L = ip device tracking
T = ip device tracking probe delay 5
T = ip device tracking
N = ip device tracking probe delay 5
N = ip device tracking
T = vtp interface Vlan11
T = #VTP domain
T = #VTP Password
T = vtp mode client
common = authentication mac-move permit
common = authentication critical recovery delay 1000
common = epm logging
common = mls qos
common = dot1x system-auth-control
common = dot1x guest-vlan supplicant
common = dot1x critical eapol
common = spanning-tree mode rapid-pvst
common = vlan internal allocation policy ascending
common = ip tcp synwait-time 10
common = ip tftp blocksize 8192
common = interface FastEthernet0
common = no ip address
common = shutdown
common = interface Vlan1
common = no ip address
common = shutdown
common = interface Vlan11
common = description Management Interface
common = #IP address
common = no ip redirects
common = no ip unreachables
common = no ip proxy-arp
common = no ip route-cache
common = #IP default-gateway
common = ip http server
common = no ip http secure-server
common = ip http secure-active-session-modules none
common = ip http active-session-modules none
common = crypto key generate rsa general keys modulus 1024
common = ip ssh time-out 10
common = ip ssh version 2
L = ip access-list standard L_SNMP_Traffic
T = ip access-list standard T_SNMP_Traffic
N = ip access-list standard N_SNMP_Traffic
common = permit 10.21.8.90
common = permit 10.20.9.190
L = permit ip 10.22.101.100
T = permit ip 10.21.5.105
N = permit ip 10.21.20.250
L = permit ip 10.2.0.32
T = permit ip 10.10.5.100
N = permit ip 10.2.0.32
common = deny any
L = ip access-list standard L_SSH_Traffic
T = ip access-list standard T_SSH_Traffic
N = ip access-list standard N_SSH_Traffic
common = permit 10.2.0.0 0.0.255.255
common = permit 10.8.0.0 0.0.255.255
common = permit 10.20.0.0 0.0.255.255
common = permit 10.21.0.0 0.0.255.255
common = permit 10.22.0.0 0.0.255.255
common = deny any
L = ip access-list extended L_PreAuth_Traffic
T = ip access-list extended T_PreAuth_Traffic
N = ip access-list extended N_PreAuth_Traffic
common = permit udp any eq bootpc any eq bootps
common = permit icmp any any
L = permit udp any host 10.21.11.10 eq domain
T = permit udp any host 10.20.58 .6 eq domain
N = permit udp any host 10.21.20.12 eq domain
L = permit udp any host 10.22.11.11 eq domain
T = permit udp any host 10.21.9.6 eq domain
N = permit udp any host 10.21.20.13 eq domain
L = permit udp any host 10.22.1.166 eq domain
T = permit udp any host 10.20.58.165 eq domain
N = permit udp any host 10.20.8.165 eq domain
T = permit udp any host 10.21.59.165 eq domain
N = permit udp any host 10.20.9.165 eq domain
L = permit ip any host 10.22.101.100
T = permit ip any host 10.20.5.105
N = permit ip any host 10.21.20.250
L = permit ip any host 10.2.0.32
T = permit ip any host 10.20.9.105
N = permit ip any host 10.2.0.32
common = deny ip any any
L = ip access-list extended L_Redirect_Traffic
T = ip access-list extended T_Redirect_Traffic
N = ip access-list extended N_Redirect_Traffic
common = deny udp any any eq domain
common = deny ip any 10.0.0.0 0.0.0.255
common = permit tcp any any eq www
common = permit tcp any any eq 443
common = permit tcp any any eq 8080
common = ip radius source-interface Vlan11 
common = logging origin-id hostname
common = logging source-interface Vlan11
common = logging host 10.21.5.90
common = logging host 10.20.9.190
common = snmp-server view no-table iso included
common = snmp-server view no-table at excluded
common = snmp-server view no-table snmpUsmMIB excluded
common = snmp-server view no-table snmpVacmMIB excluded
common = snmp-server view no-table snmpCommunityMIB excluded
common = snmp-server view no-table ip.21 excluded
common = snmp-server view no-table ip.22 excluded
L = snmp-server community public RO L_SNMP_Traffic
T = snmp-server community public RO T_SNMP_Traffic
N = snmp-server community public RO N_SNMP_Traffic
L = snmp-server community public RO L_SNMP_Traffic
T = snmp-server community public RO T_SNMP_Traffic
N = snmp-server community public RO N_SNMP_Traffic
L = snmp-server location sitting at the doc of the bay
T = snmp-server location sitting at the doc of the bay
N = snmp-server location sitting at the doc of the bay
L = snmp-server contact this@email.me
T = snmp-server contact this@email.me
N = snmp-server contact this@email.me
common = snmp-server enable traps snmp authentication linkdown linkup coldstart warmstart
common = snmp-server enable traps transceiver all
common = snmp-server enable traps call-home message-send-fail server-fail
common = snmp-server enable traps tty
common = snmp-server enable traps license
common = snmp-server enable traps auth-framework sec-violation
common = snmp-server enable traps config-copy
common = snmp-server enable traps config
common = snmp-server enable traps config-ctid
common = snmp-server enable traps fru-ctrl
common = snmp-server enable traps entity
common = snmp-server enable traps event-manager
common = snmp-server enable traps power-ethernet group 1
common = snmp-server enable traps power-ethernet group 2
common = snmp-server enable traps power-ethernet police
common = snmp-server enable traps cpu threshold
common = snmp-server enable traps vstack
common = snmp-server enable traps bridge newroot topologychange
common = snmp-server enable traps stpx inconsistency root-inconsistency loop-inconsistency
common = snmp-server enable traps syslog
common = snmp-server enable traps vtp
common = snmp-server enable traps vlancreate
common = snmp-server enable traps vlandelete
common = snmp-server enable traps flash insertion removal
common = snmp-server enable traps port-security
common = snmp-server enable traps envmon fan shutdown supply temperature status
common = snmp-server enable traps stackwise
common = snmp-server enable traps errdisable
common = snmp-server enable traps mac-notification change move threshold
common = snmp-server enable traps vlan-membership
common = radius-server attribute 6 on-for-login-auth
common = radius-server attribute 6 support-multiple
common = radius-server attribute 8 include-in-access-req
common = radius-server attribute 25 access-request include
common = radius-server attribute 31 mac format ietf 
common = radius-server dead-criteria time 1 tries 3
common = radius-server retry method reorder
common = radius-server timeout 3
common = radius-server vsa send cisco-nas-port
L = radius server l-isepsn-01
L = address ipv4 10.22.11.10 auth-port 1645 acct-port 1646
L = key 7 12345678901234
L = radius server fp-isepsn-01
L = address ipv4 10.2.0.2 auth-port 1645 acct-port 1646
L = key 7 12345678901234
T = radius server t-isepsn-01
T = address ipv4 10.21.5.105 auth-port 1645 acct-port 1646
T = key 7 12345678901234
T = radius server t-isepsn-02
T = address ipv4 10.21.5.15 auth-port 1645 acct-port 1646
T = key 7 12345678901234
N = radius server n-isepsn-01
N = address ipv4 10.2.20.25 auth-port 1645 acct-port 1646
N = key 7 12345678901234
N = radius server fp-isepsn-01
N = address ipv4 10.2.0.2 auth-port 1645 acct-port 1646
N = key 7 12345678901234
common = line con 0
common = exec-timeout 30 0
common = logging synchronous
common = line vty 0 15
L = access-class L_SSH_Traffic in
T = access-class T_SSH_Traffic in
N = access-class N_SSH_Traffic in
common = exec-timeout 30 0
common = logging synchronous
common = transport input ssh
common = ntp source Vlan11
L = ntp server 10.21.11.1 prefer
T = ntp server 10.20.5.6 prefer
N = ntp server 10.21.20.12 prefer
L = ntp server 10.22.101.11
T = ntp server 10.20.9.6
N = ntp server 10.21.20.1
common = mac address-table notification change interval 0
common = mac address-table notification change
common = mac address-table notification mac-move